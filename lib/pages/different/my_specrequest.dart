import 'package:flutter/material.dart';

class MySpecRequestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('My special requests'),
      ),
      body: Center(
        child: Text('My special requests'),
      ),
    );
  }
}