import 'package:flutter/material.dart';

class OurAddressPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Our Address'),
      ),
      body: Center(
        child: Text('Our Address'),
      ),
    );
  }
}